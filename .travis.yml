language: node_js
node_js:
  - "9"
dist: trusty
sudo: required
branches:
  only:
  - master
before_script:
  - npm install -g @angular/cli
  - npm install -g firebase-tools
  - npm install --prefix ./functions
script:
- ng lint --prefix ./client
- ng build --prefix ./client
after_success:
- firebase deploy --token $FIREBASE_TOKEN
